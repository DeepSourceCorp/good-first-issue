[project]
name = "good-first-issue"
version = "0.1.0"
description = "Trial contribution by deepanshi254025"
license = "MIT"
authors = [
    {name = "Deepanshi", email = "deepanshi_254025@saitm.ac.in@gmail.com"}
]
requires-python = ">=3.9"
dependencies = [
    "github3.py>=4.0.1",
    "toml>=0.10.2",
    "numerize>=0.12",
    "emoji>=0.6.0",
    "requests>=2.23.0",
    "python-slugify>=4.0.1",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.1.8",
    "types-toml>=0.10.8.7",
    "types-python-slugify>=8.0.0.3",
    "loguru>=0.7.2",
    "mypy>=1.7.1",
    "black>=24.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["gfi"]

[tool.ruff]
line-length = 120

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
